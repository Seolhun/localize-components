import {
  preventDefault,
  addEvent,
  removeEvent,
} from './utils/common';

import {
  preventWindowScroll,
} from './utils/scroll';

import {
  getClientWindowSize,
  getScrollSize
} from './utils/window';

export {
  // common
  preventDefault,
  addEvent,
  removeEvent,
  // scroll
  preventWindowScroll,
  // window
  getClientWindowSize,
  getScrollSize
};

export default {
  // common
  preventDefault,
  addEvent,
  removeEvent,
  // scroll
  preventWindowScroll,
  // window
  getClientWindowSize,
  getScrollSize
};

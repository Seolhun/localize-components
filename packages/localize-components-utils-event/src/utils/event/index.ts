import { preventDefault, addEvent, removeEvent } from './CommonEventUtils';
import { preventWindowScroll } from './ScrollUtils';
import { getTouchDirection, getTouchData } from './TouchEventUtils';

export {
  preventDefault,
  addEvent,
  removeEvent,
  // Touch
  getTouchDirection,
  getTouchData,
  // Scroll
  preventWindowScroll,
}

export default {
  preventDefault,
  addEvent,
  removeEvent,
  // Touch
  getTouchDirection,
  getTouchData,
  // Scroll
  preventWindowScroll,
};

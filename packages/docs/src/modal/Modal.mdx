---
name: Modal
menu: Components
---

import { useState } from 'react';
import { Playground, Props } from 'docz';
import {
  LocalizeModal,
  LocalizeThemeProvider,
  useDisclosure,
} from '@seolhun/localize-components';
import { LocalizeButton } from '@seolhun/localize-components-atomic';

import ModalPropsTable from './ModalPropsTable';

# Modal

## Caution
- [Docz - RoadMap](http://feedback.docz.site/roadmap?sort=trending)

> Docz is not supported knobs(Under Review) not yet. If you want to see this, add isShow Props.

## Default

<Playground>
  {() => {
    const { isOpen, onToggle } = useDisclosure();

    return (
      <LocalizeThemeProvider>
        <LocalizeButton onClick={onToggle}>
          {isOpen ? 'Show Modal' : 'Close Modal'}
        </LocalizeButton>
        <LocalizeModal isShow={isOpen} onClose={onToggle}>
          <div>LocalizeModal header</div>
          <div>LocalizeModal footer</div>
          <div>LocalizeModal body</div>
        </LocalizeModal>
      </LocalizeThemeProvider>
    )
  }}
</Playground>

## PropsTable

<Props of={ModalPropsTable} />

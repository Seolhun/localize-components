import React, { FC } from 'react';

import { LocalizeIconProps } from '@seolhun/localize-components';

const LocalizeIconPropsTable: FC<LocalizeIconProps> = () => {
  return <div />;
};

export default LocalizeIconPropsTable;

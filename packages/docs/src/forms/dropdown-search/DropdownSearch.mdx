---
name: DropdownSearch
menu: Forms
---

import { Playground, Props } from 'docz';
import { LocalizeDropdownSearch } from '@seolhun/localize-components-dropdown';
import { LocalizeThemeProvider } from '@seolhun/localize-components';


# DropdownSearch

## Default

<Playground>
  <LocalizeThemeProvider>
      <LocalizeDropdownSearch
        items={['Hi', 'Halo', '안녕하세요.']}
        onClickItem={(item) => {
          console.error(item)
        }}
        renderItem={(item) => (
          <div>{item}</div>
        )}
      />
  </LocalizeThemeProvider>
</Playground>

## PropsTable

<Props of={LocalizeDropdownSearch} />

---
name: DropdownSearch
menu: Forms
---

import { Playground, Props } from 'docz';
import { LocalizeDropdownSearch, LocalizeSearchResult } from '@seolhun/localize-components';
import { LocalizeThemeProvider } from '@seolhun/localize-components';


# DropdownSearch

## Default

<Playground>
  <LocalizeThemeProvider>
      <LocalizeDropdownSearch>
        {({ onSetIsShowResult }) => (
          <LocalizeSearchResult
            items={['Hi', 'Halo', '안녕하세요.']}
            onClickItem={(item) => {
              console.error(item)
            }}
            renderItem={(item) => (
              <div>{item}</div>
            )}
          />
        )}
      </LocalizeDropdownSearch>
  </LocalizeThemeProvider>
</Playground>

## PropsTable

<Props of={LocalizeDropdownSearch} />

---
name: Input
menu: Forms
---

import { Playground, Props } from 'docz';
import { LocalizeInput } from '@seolhun/localize-components-atomic';
import { LocalizeThemeProvider } from '@seolhun/localize-components';

# Input

## Default

<Playground>
  <LocalizeThemeProvider>
    <LocalizeInput value='' placeholder='Localize Component' />
    <LocalizeInput value='' subColor='#000' placeholder='Localize Component' />
  </LocalizeThemeProvider>
</Playground>

## Give a renderValue

<Playground>
  <LocalizeThemeProvider>
    <LocalizeInput value='Value' renderValue={(value) => `Custom_${value}`} />
    <LocalizeInput value='Value' renderValue={(value) => (
      `<button>${value}</button>`
    )} />
  </LocalizeThemeProvider>
</Playground>

## PropsTable

<Props of={LocalizeInput} />

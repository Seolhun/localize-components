---
name: CheckBoxGroup
menu: Forms
---

import { Playground, Props } from 'docz';
import { LocalizeCheckBoxGroup, LocalizeCheckBox } from '@seolhun/localize-components-atomic';
import { LocalizeThemeProvider } from '@seolhun/localize-components';


# CheckBoxGroup

## Default

<Playground>
  <LocalizeThemeProvider>
    <LocalizeCheckBoxGroup groupName='localize'>
      {props => (
        <>
          <LocalizeCheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <LocalizeCheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
        </>
      )}
    </LocalizeCheckBoxGroup>
  </LocalizeThemeProvider>
</Playground>

## Give a align

<Playground>
  <LocalizeThemeProvider>
    <LocalizeCheckBoxGroup
      groupName='localize4'
      align='vertical'
    >
      {props => (
        <>
          <LocalizeCheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <LocalizeCheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
        </>
      )}
    </LocalizeCheckBoxGroup>
    <LocalizeCheckBoxGroup
      groupName='localize7'
      align='horizontal'
    >
      {props => (
        <>
          <LocalizeCheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <LocalizeCheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
        </>
      )}
    </LocalizeCheckBoxGroup>
  </LocalizeThemeProvider>
</Playground>

## Give a align & gap

<Playground>
  <LocalizeThemeProvider>
    <LocalizeCheckBoxGroup
      groupName='localize7'
      align='horizontal'
      gap='30px'
    >
      {props => (
        <>
          <LocalizeCheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <LocalizeCheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
        </>
      )}
    </LocalizeCheckBoxGroup>
    <LocalizeCheckBoxGroup
      groupName='localize7'
      align='horizontal'
      gap='10em'
    >
      {props => (
        <>
          <LocalizeCheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <LocalizeCheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <LocalizeCheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
        </>
      )}
    </LocalizeCheckBoxGroup>
  </LocalizeThemeProvider>
</Playground>

## PropsTable

<Props of={LocalizeCheckBoxGroup} />

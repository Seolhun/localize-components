---
name: CheckBoxGroup
menu: Forms
---

import { Playground, Props } from 'docz';
import { CheckBoxGroup, CheckBox } from '@seolhun/localize-components-atomic';
import { LocalizeThemeProvider } from '@seolhun/localize-components';


# CheckBoxGroup

## Default

<Playground>
  <LocalizeThemeProvider>
    <CheckBoxGroup groupName='localize'>
      {props => (
        <>
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
        </>
      )}
    </CheckBoxGroup>
  </LocalizeThemeProvider>
</Playground>

## Give a mainColor

<Playground>
  <LocalizeThemeProvider>
    <CheckBoxGroup
      groupName='localize2'
      mainColor='danger'
    >
      {props => (
        <>
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
        </>
      )}
    </CheckBoxGroup>
  </LocalizeThemeProvider>
</Playground>

## Give a subColor

<Playground>
  <LocalizeThemeProvider>
    <CheckBoxGroup
      groupName='localize3'
      mainColor='danger'
      subColor='yellow'
    >
      {props => (
        <>
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
        </>
      )}
    </CheckBoxGroup>
  </LocalizeThemeProvider>
</Playground>

## Give a align

<Playground>
  <LocalizeThemeProvider>
    <CheckBoxGroup
      groupName='localize4'
      mainColor='danger'
      align='vertical'
    >
      {props => (
        <>
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
        </>
      )}
    </CheckBoxGroup>
    <CheckBoxGroup
      groupName='localize7'
      mainColor='green'
      align='horizontal'
    >
      {props => (
        <>
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
        </>
      )}
    </CheckBoxGroup>
  </LocalizeThemeProvider>
</Playground>

## Give a align & gap

<Playground>
  <LocalizeThemeProvider>
    <CheckBoxGroup
      groupName='localize7'
      mainColor='black'
      align='horizontal'
      gap='30px'
    >
      {props => (
        <>
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
        </>
      )}
    </CheckBoxGroup>
    <CheckBoxGroup
      groupName='localize7'
      mainColor='sky'
      align='horizontal'
      gap='10em'
    >
      {props => (
        <>
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
          <CheckBox
            {...props}
            item={{
              label: 'label',
              value: 'value',
            }}
            checked
          />
        </>
      )}
    </CheckBoxGroup>
  </LocalizeThemeProvider>
</Playground>

## PropsTable

<Props of={CheckBoxGroup} />

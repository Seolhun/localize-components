---
name: Switch
menu: Forms
---

import { Playground, Props } from 'docz';
import { LocalizeSwitch } from '@seolhun/localize-components-atomic';
import { LocalizeThemeProvider, lightTheme, darkTheme } from '@seolhun/localize-components';

# Switch

## Default

<Playground>
  {() => {
    const [isChecked, setIsChecked] = React.useState(false)
    return (
      <LocalizeThemeProvider theme={isChecked ? lightTheme : darkTheme}>
        <LocalizeSwitch
          htmlFor='test'
          checked={isChecked}
          onChange={(checked) => setIsChecked(event.target.checked)}
        />
      </LocalizeThemeProvider>
    )
  }}
</Playground>

## PropsTable

<Props of={LocalizeSwitch} />

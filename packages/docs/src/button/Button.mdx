---
name: Button
menu: Atomic
---

import { Playground, Props } from 'docz';
import { LocalizeButton } from '@seolhun/localize-components-atomic';

import { ThemeChangerProvider } from '../ThemeChangerProvider'

# Button

## Default

<Playground>
  <ThemeChangerProvider>
    <LocalizeButton>Click me</LocalizeButton>
  </ThemeChangerProvider>
</Playground>

## Disabled

<Playground>
  <ThemeChangerProvider>
    <LocalizeButton disabled>Disabled Click</LocalizeButton>
  </ThemeChangerProvider>
</Playground>

## Give a size

<Playground>
  <ThemeChangerProvider>
    <LocalizeButton size='small'>Click me</LocalizeButton>
    <LocalizeButton size='medium'>Click me</LocalizeButton>
    <LocalizeButton size='large'>Click me</LocalizeButton>
  </ThemeChangerProvider>
</Playground>

## PropsTable

<Props of={LocalizeButton} />

---
name: Button
menu: Atomic
---

import { Playground, Props } from 'docz';
import { Button } from '@seolhun/localize-components-atomic';
import { ThemeProvider } from 'emotion-theming';
import { LocalizeTheme } from '@seolhun/localize-components';

import ButtonPropsTable from './ButtonPropsTable';

# Button

## Default

<Playground>
  <ThemeProvider theme={LocalizeTheme}>
    <Button>Click me</Button>
  </ThemeProvider>
</Playground>

## Give a mainColor

<Playground>
  <ThemeProvider theme={LocalizeTheme}>
    <Button mainColor='primary'>primary</Button>
    <Button mainColor='danger'>danger</Button>
    <Button mainColor='light_grey'>light_grey</Button>
    <Button mainColor='#41FF2B'>#41FF2B</Button>
  </ThemeProvider>
</Playground>

## Give a size

<Playground>
  <ThemeProvider theme={LocalizeTheme}>
    <Button size='small'>Click me</Button>
    <Button size='medium'>Click me</Button>
    <Button size='large'>Click me</Button>
  </ThemeProvider>
</Playground>

## PropsTable

<Props of={ButtonPropsTable} />

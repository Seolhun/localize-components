import Alert, { AlertProps } from './alert';
import {
  Confirm,
  ConfirmProps,
  InputConfirm,
  InputConfirmProps,
} from './confirm';
import Modal, { ModalProps } from './modal';

export {
  Alert,
  AlertProps,
  Confirm,
  ConfirmProps,
  InputConfirm,
  InputConfirmProps,
  Modal,
  ModalProps,
};

export default {
  Alert,
  Confirm,
  InputConfirm,
  Modal,
};

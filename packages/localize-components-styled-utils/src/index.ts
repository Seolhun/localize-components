// Position
import {
  getBottomPositionStyle,
  getPositionStyle,
} from './utils/styles/PositionUtils';
// Theme
import {
  getIsLightenTheme,
  getValidTheme,
} from './utils/styles/ThemeUtils';

export {
  // Position
  getBottomPositionStyle,
  getPositionStyle,
  // Theme
  getIsLightenTheme,
  getValidTheme,
};

export default {
  // Position
  getBottomPositionStyle,
  getPositionStyle,
  // Theme
  getIsLightenTheme,
  getValidTheme,
};

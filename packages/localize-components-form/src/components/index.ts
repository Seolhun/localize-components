import CheckBox, { CheckBoxProps } from './checkbox';
import Input, { InputProps } from './Input';

export {
  Input,
  InputProps,
  CheckBox,
  CheckBoxProps,
};

export default {
  Input,
  CheckBox,
};

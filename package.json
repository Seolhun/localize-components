{
  "private": true,
  "author": "Seolhun",
  "name": "@seolhun/localize-components",
  "version": "0.0.6",
  "description": "Localizing React components is rendered by stand-alone not affect other styles.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Seolhun/localize-components"
  },
  "homepage": "https://github.com/Seolhun/localize-components#readme",
  "bugs": {
    "url": "https://github.com/Seolhun/localize-components/issues"
  },
  "keywords": [
    "react",
    "component",
    "typescript",
    "ui",
    "scss",
    "local"
  ],
  "license": "MIT",
  "main": "./src/index.ts",
  "scripts": {
    "clean": "lerna clean",
    "bs": "lerna bootstrap",
    "packages": "run-s packages:*",
    "packages:bs": "lerna bootstrap",
    "packages:fix": "lerna run fix && echo",
    "packages:lint": "lerna run tslint.json",
    "packages:build": "yarn build && lerna run build",
    "prerelease": "yarn run packages",
    "release": "lerna publish --conventional-commits",
    "release:beta": "yarn release --npm-tag=beta --preid=beta"
  },
  "dependencies": {
    "enzyme": "^3.4.4",
    "enzyme-adapter-react-16": "^1.2.0",
    "jest": "^23.5.0"
  },
  "devDependencies": {
    "@types/jest": "23.3.1",
    "codacy-coverage": "3.0.0",
    "lerna": "^3.1.4",
    "mocha-lcov-reporter": "1.3.0",
    "ts-jest": "23.1.3",
    "tslint-config-airbnb": "^5.10.0",
    "typescript": "3.0.1"
  },
  "peerDependencies": {
    "concurrently": "^4.0.0",
    "react": "^16.4.2",
    "react-dom": "^16.4.2"
  },
  "workspaces": [
    "packages/*"
  ],
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">= 6.0.0",
    "npm": ">= 3.0.0"
  }
}
